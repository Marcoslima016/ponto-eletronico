import 'package:flutter/material.dart';

class EditTimeView extends StatelessWidget {
  double w;
  double h;
  @override
  Widget build(BuildContext context) {
    h = MediaQuery.of(context).size.height / 100;
    w = MediaQuery.of(context).size.width / 100;
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
